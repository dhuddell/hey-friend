'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _propTypes = require('prop-types');

var _propTypes2 = _interopRequireDefault(_propTypes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const PhaseCardWhen = ({ children, phaseConditions }, { phaseState }) => {
  if (phaseConditions === '*') {
    return children;
  }

  if (phaseConditions === phaseState) {
    return children;
  }

  if (Array.isArray(phaseConditions)) {
    return phaseConditions.indexOf(phaseState) > -1 ? children : null;
  }

  return null;
};

PhaseCardWhen.defaultProps = {
  phaseConditions: []
};

PhaseCardWhen.propTypes = {
  children: _propTypes2.default.node,
  phaseConditions: _propTypes2.default.oneOfType([_propTypes2.default.string, _propTypes2.default.arrayOf(_propTypes2.default.string)])
};

PhaseCardWhen.contextTypes = {
  phaseState: _propTypes2.default.string
};

exports.default = PhaseCardWhen;