query UpdateFriendGoals($id: String!, $goals: Goals!) {
  friend(id: $id, goals: $goals){
    id
    goals {
      targetGoal {
        phone
        text
        beer
      }
      currentGoal {
        phone
        text
        beer
      }
      cadence
    }
  }
}