'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _propTypes = require('prop-types');

var _propTypes2 = _interopRequireDefault(_propTypes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const ExpanderWhen = ({ children, showCollapse = true, showExpanded = true }, { isExpanded }) => {
  if (showCollapse && showExpanded) {
    return children;
  }

  if (showExpanded && isExpanded) {
    return children;
  }

  if (showCollapse && !isExpanded) {
    return children;
  }

  return null;
};

ExpanderWhen.propTypes = {
  children: _propTypes2.default.node,
  showCollapse: _propTypes2.default.bool,
  showExpanded: _propTypes2.default.bool
};

ExpanderWhen.contextTypes = {
  isExpanded: _propTypes2.default.bool.isRequired
};

exports.default = ExpanderWhen;